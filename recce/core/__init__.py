"""Core recce data model + shared utilities (moved from top-level modules).

Historically these lived at `recce.models`, `recce.store`, `recce.tracking`,
etc. — the top-level shims in `recce/*.py` still re-export from here so old
imports keep working.
"""
