"""recce web workbench - a local FastAPI app that serves an engagement to a browser.

Bundled and airgap-native: one recce instance runs on the operator's box; teammates
open it in a browser over the engagement LAN. The read-only API here is Phase 1; live
scans, progress ticks and multi-tester collaboration build on top.
"""
