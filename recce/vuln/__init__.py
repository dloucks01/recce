"""Vulnerability matching, proofs, exploit reference tables, and NSE
verification rules. The core matching happens in `vulndb.py`; the human-
facing narratives + msf module references live in `proofs.py` and
`exploitref.py`.
"""
