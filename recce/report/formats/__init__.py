"""Low-level format writers — raw .docx / .xlsx bytes with no domain knowledge.

The renderers in ../{docx,excel,markdown,html}.py compose these with
engagement data. Nothing outside the report subpackage should reach in here.
"""
